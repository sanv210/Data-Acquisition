# to start the virtual env: venv\Scripts\activate


cd connection
# from repo root
python connection\\integration.py

# or from inside connection folder
python integration.py

# to change the backend base url
python integration.py --base-url http://localhost:8000